interface:
  display_name: "Codegraph"
  short_description: "Repo-specific patterns and workflows for codegraph"
  default_prompt: "Use the codegraph repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true